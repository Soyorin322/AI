name: Reirin speaker annotation

on:
  push:
    paths:
      - '.github/workflows/reirin-speaker-annotation.yml'
      - 'character_data/tools/generate_reirin_speaker_annotations.py'
      - 'character_data/Reirin/sources/raw/novel/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  annotate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Generate speaker annotations
        run: python character_data/tools/generate_reirin_speaker_annotations.py
      - name: Commit generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- 'character_data/Reirin/sources/raw/novel/惡女不才_第二卷_speaker重校.md' 'character_data/Reirin/sources/raw/novel/惡女不才_第三卷_speaker重校.md'
          if git diff --cached --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git commit -m "Generate Reirin volume 2 and 3 speaker annotations [skip ci]"
          git push
